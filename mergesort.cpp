#include <iostream>
using namespace std;

 //create main a
 int arr[20], B[20];
 int n;


void mergeSort(int low, int high)
{
   
   if (low >= high)
   {
        return;
   }
   
   int mid = (low + high)/2; //steps 2
}